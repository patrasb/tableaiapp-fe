import CreditBadge from '../CreditBadge';
import UploadSection from '../UploadSection';

export default function UploadCard() {
  return (
    <CreditBadge>
      <UploadSection variant='card' />
    </CreditBadge>
  );
}
